services:
  - type: web
    name: shipflow-ci
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/shipflow-0.0.1-SNAPSHOT.jar --server.port=${PORT:-8080}
    envVars:
      - key: JAVA_VERSION
        value: 17
